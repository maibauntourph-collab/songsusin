# Real-time Mobile Guide Audio System

**1 Guide (가이드) - 60 Tourists (관광객) 실시간 오디오 중계 및 자동 번역 시스템**  
Python FastAPI, WebRTC, Socket.IO를 사용하여 모바일 환경(Android/iOS)에서 안정적인 오디오 스트리밍과 실시간 자막(STT)을 제공합니다.

---

## 🚀 주요 기능 (Key Features)

### 1. 🎧 하이브리드 오디오 스트리밍 (Hybrid Audio)
*   **WebRTC + WebSocket 듀얼 모드**: 로컬 네트워크(Wifi) 환경에서 WebRTC 연결이 실패하더라도, WebSocket을 통해 끊김 없이 오디오를 전달합니다.
*   **Android/iOS 호환성**: 아이폰(iOS)와 안드로이드 모두에서 별도 앱 설치 없이 **크롬/사파리 브라우저**만으로 작동합니다.

### 2. 📝 클라이언트 사이드 번역 (Client-Side Translation)
*   **서버 부하 제로**: 가이드의 말(Korean)을 서버가 아닌 **각 관광객의 폰에서 직접 번역**합니다.
*   **100개 국어 지원**: 영어, 중국어, 일본어 등 100개 이상의 언어를 지원하며, 서버 성능 저하 없이 60명 이상의 동시 접속을 처리합니다.

### 3. 🤖 AI 모드 vs 라이브 모드 (Toggle)
*   **Live Voice (기본)**: 가이드의 생생한 목소리를 직접 듣습니다. (자막은 참고용)
*   **AI Voice**: 가이드 목소리를 **음소거**하고, AI가 번역된 언어로 읽어줍니다. (외국인 관광객 추천 🌟)

### 4. 📱 모바일 서버 최적화
*   노트북 없이 **가이드의 핸드폰**을 서버로 사용할 수 있습니다 (Termux/UserLAnd 활용).
*   **HTTPS 자동 적용**: 마이크 권한 문제 해결을 위해 자체 서명된 SSL 인증서를 자동으로 생성합니다.
*   **QR 코드 접속**: 서버 실행 시 접속용 QR 코드가 터미널에 바로 표시됩니다.

---

## 🛠 설치 및 실행 (Setup & Run)

### 1. PC/노트북에서 실행 시
```bash
# 의존성 설치
pip install -r requirements.txt

# 서버 실행
python server.py
```
*   **접속 주소**: 터미널에 표시된 `https://192.168.x.x:5000` (QR코드 스캔 가능)

### 2. 안드로이드 폰에서 실행 시 (Mobile Server)
*   `PHONE_SERVER_SETUP.md` (Termux) 또는 `USERLAND_SETUP.md` (Ubuntu 앱) 가이드를 참고하세요.
*   **핵심**: 폰에서 서버를 켜고, 핫스팟이나 공유기를 통해 관광객과 연결합니다.

---

## 📱 사용 방법 (User Guide)

### 1. 가이드 (Host)
1.  브라우저로 `https://localhost:5000` 접속 (서버 폰 본인).
2.  **"I am Guide"** 선택 -> **"Start Broadcast"** 클릭.
3.  마이크 권한 허용.

### 2. 관광객 (Guest)
1.  가이드와 **같은 와이파이**에 연결.
2.  가이드가 알려주는 주소(예: `https://192.168.0.15:5000`) 또는 QR코드로 접속.
3.  **경고창 통과**: "안전하지 않음(Not Secure)" -> [고급] -> [이동] 클릭 (필수).
4.  **"I am Tourist"** 선택 -> **"Tap to Enable Audio"** 클릭.
5.  **언어 선택**: 원하는 언어(예: English) 선택 시 자동 번역됨.
6.  **모드 전환**: "🎧 Live Voice" 버튼을 눌러 "🤖 AI Voice"로 변경 가능.

---

## ⚠️ 문제 해결 (Troubleshooting)

*   **Q. localhost 접속이 안 돼요!**
    *   반드시 **`https://`** 를 붙여야 합니다. (`http` 아님)
    
*   **Q. 소리가 안 들려요!**
    *   "Tap to Enable Audio" 버튼을 눌렀는지 확인하세요.
    *   **"Hybrid: ... + WS"** 문구가 가이드 화면에 뜨는지 확인하세요. (자동 복구 모드)

*   **Q. AI 목소리가 안 나와요.**
    *   언어를 선택했는지 확인하세요. (Original 상태에선 안 나옴)
    *   "AI Voice" 버튼이 켜져 있는지 확인하세요.
